# HTML Template Literal Extension

VS Code extension for enhanced HTML support in template literals with folding, IntelliSense, and validation.

## Project Structure
- TypeScript-based VS Code extension
- Provides folding for tagged template literals (dom, html, etc.)
- HTML IntelliSense and validation inside template literals
- Configurable tag detection
